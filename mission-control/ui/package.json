{
  "name": "mission-control-kanban-workspace",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix app",
    "dev": "concurrently -k -n server,client -c cyan,green \"npm run dev --prefix server\" \"npm run dev --prefix app\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix app"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
